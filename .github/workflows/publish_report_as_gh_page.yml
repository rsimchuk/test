name: Publist Report As GH Page

on:
  push:
    branches: ["main"]
  workflow_dispatch:

#permissions:
#  id-token: write
#  pages: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          mkdir report
          python -m pip install -U pip
          python -m pip install -r req.txt
      - name: Test it
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        continue-on-error: true
        run: |
          echo "\nTest" >> README.md
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@visual-layer.com"
          git add .
          git commit -m "Update Readme file"

          git push https://x-access-token:e30392cca6d51358eef876171843dfa712593765eec87ccbe7ac72163335f050@github.com/${{ github.repository }}.git HEAD:main
#          pytest tests/ --junitxml=report/report.xml

#      - uses: actions/upload-artifact@v4
#        with:
#          name: test_report
#          path: report
#          if-no-files-found: error
